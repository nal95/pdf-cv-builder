spring:
  datasource:
    url: jdbc:h2:mem:cvs
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true

server:
  servlet:
    context-path: '/api/v1/pdf-cv'